# Bildmodell
torch
# >=0.36: diffusion.py laedt Flux2KleinPipeline (FLUX.2), die es erst ab
# dieser Version gibt - siehe die gleiche Untergrenze in docker-compose.yml.
diffusers>=0.36
transformers
accelerate
safetensors
pillow

# Ausgabeschema: schema.py definiert damit das Ausgabeformat, das als
# Grammatik an den Server geht und die Antwort validiert.
pydantic>=2.7

# vLLM-Container aus dem Spiel heraus starten (llm.VLLM._serve)
docker
